NEXT_PUBLIC_API_URL="http://localhost:4000"
# Socket.IO endpoint; defaults to NEXT_PUBLIC_API_URL when unset (the gateway
# is served by the same Nest process).
NEXT_PUBLIC_WS_URL="http://localhost:4000"
